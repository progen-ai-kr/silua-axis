# 제품 등록·수정 안내 (브랜드용)

이 사이트에는 **제품 관리자 페이지**가 내장되어 있습니다.
쇼핑몰 관리자처럼 **사이트 안에서** 제품을 추가·수정·삭제할 수 있고, 저장하면 **1~2분 뒤 자동으로 반영**됩니다.

## 관리자 페이지 여는 법

1. 브라우저에서 **`사이트주소/admin.html`** 접속
   (예: `https://우리사이트.progen-web.workers.dev/admin.html`)
2. **처음 한 번만**: 운영진(프로젠)에게 받은 **접속키**를 붙여넣고 [연결하기]
3. 이후에는 같은 브라우저에서 바로 관리 화면이 열립니다.

## 할 수 있는 것

| 작업 | 방법 |
|---|---|
| **제품 추가** | [＋ 새 제품] → 이름·설명·키워드 입력 → 사진 선택(자동 업로드) → [저장하기] |
| **제품 수정** | 목록에서 [수정] → 내용 고치고 [저장하기] |
| **제품 삭제** | 목록에서 [삭제] |
| **순서 바꾸기** | [↑][↓] — 위에서 4개가 홈 화면에 나옵니다 |
| **사진 관리** | 편집 화면에서 사진 추가(파일 선택) / ✕로 제거. **첫 번째 사진 = 대표 사진** |

- 사진은 **세로형(3:4)**, 가로 1200px 이상 권장
- 상세 설명에서 **빈 줄 두 번** = 문단 나눔
- 저장 후 사이트 반영까지 **1~2분** 걸립니다 (새로고침으로 확인)

## 주의

- **접속키는 비밀번호처럼 보관**하세요. 다른 사람에게 공유하지 마세요.
- 접속키를 잃어버렸거나 "연결이 만료되었어요"가 나오면 → 운영진에게 재발급 요청
- 관리자 페이지가 아닌 다른 파일(코드)은 수정하지 마세요.

문의: 프로젠 (suhun@progen.ai.kr)

---

## (운영진용) 접속키 발급 방법

브랜드별 접속키 = **GitHub Fine-grained 토큰** (해당 레포 1개만, Contents 권한만):

1. github.com (suhun-progen 로그인) → **Settings → Developer settings → Personal access tokens → Fine-grained tokens → Generate new token**
2. 설정:
   - Token name: `admin-{레포이름}` / Expiration: **1년(최대)**
   - Resource owner: **progen-ai-kr**
   - Repository access: **Only select repositories → 해당 브랜드 레포 1개만**
   - Permissions → Repository permissions → **Contents: Read and write** (나머지 전부 No access)
3. Generate → `github_pat_…` 복사 → **해당 브랜드에게만** 전달
4. 만료·분실 시 같은 방법으로 재발급 (기존 토큰은 revoke)

> 이 토큰은 해당 레포의 파일 읽기/쓰기만 가능하며, 조직의 다른 레포·설정에는 접근할 수 없습니다.
