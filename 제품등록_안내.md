# 제품 등록·수정 안내 (브랜드용)

이 사이트는 **제품을 코딩 없이** 직접 등록·수정할 수 있습니다.
`products.json` 파일 하나만 고치면 제품 목록·상세 페이지에 **자동으로 반영**됩니다.

## 제품 추가하는 법 (컴퓨터 브라우저에서)

1. **github.com 로그인** → 이 저장소 열기
2. 파일 목록에서 **`products.json`** 클릭 → 오른쪽 위 **연필(✏️ Edit)** 클릭
3. 기존 제품 한 덩어리( `{` 부터 `}` 까지 )를 **복사해서 아래에 붙여넣고** 내용만 수정:

```
{
  "id": "look-05",                 ← 다른 제품과 겹치지 않는 영문 이름
  "label": "LOOK 05",              ← 목록에 작게 표시되는 라벨
  "name": "제품 이름",
  "summary": "목록에 보이는 한 줄 요약",
  "description": "상세 설명. 빈 줄 두 번이면 문단이 나뉩니다.",
  "keywords": ["키워드1", "키워드2"],
  "images": ["images/look05-1.jpg"],
  "buyLink": "https://smartstore.naver.com/..."   ← 없으면 "" 로 두세요
}
```

   ⚠️ 덩어리 사이에 **쉼표(,)** 를 꼭 넣으세요. (마지막 덩어리 뒤에는 쉼표 없음)

4. 아래 **Commit changes** (초록 버튼) 클릭 → **1~2분 뒤 사이트에 자동 반영** ✅

## 사진 올리는 법

1. 저장소에서 **`images` 폴더** 클릭
2. **Add file → Upload files** → 사진 끌어다 놓기 → **Commit changes**
3. `products.json`의 `"images"` 칸에 방금 올린 **파일명**을 적으세요. (예: `"images/look05-1.jpg"`)
   - 사진은 **세로형(3:4)**, 가로 1200px 이상 권장

## 제품 수정·삭제

- **수정**: `products.json`에서 해당 제품의 글자만 고치고 Commit
- **삭제**: 해당 제품 덩어리( `{ ... },` )를 통째로 지우고 Commit

## 자주 하는 실수

| 증상 | 원인 |
|---|---|
| 저장했는데 제품이 안 뜸 | 쉼표(,) 빠짐/중복 — 덩어리 사이엔 쉼표, 마지막엔 없음 |
| 사진이 안 나옴 | `images/파일명` 오타 또는 사진 업로드 안 됨 |
| 반영이 안 된 것 같음 | 1~2분 대기 후 새로고침 (자동 배포 시간) |

문의: 프로젠 (suhun@progen.ai.kr)
