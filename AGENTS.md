# Codex 작업 지침 — 이 저장소

이곳은 **브랜드 웹사이트(5페이지 정적 사이트)**입니다. 코딩이 처음인 학생이 작업합니다.

## 사이트 구조 (브랜드 "자료 작성서"와 1:1 대응)
브랜드가 보낸 **「브랜드 웹사이트 자료 작성서」**의 각 항목은 아래 페이지의 자리에 들어갑니다.
학생이 작성서 내용을 붙여넣어 달라고 하면 이 매핑대로 넣어주세요. (각 파일 안에 `[작성서 ○○]` 주석으로 자리가 표시되어 있음)

| 작성서 항목 | 파일 | 자리 |
|---|---|---|
| 01 기본 정보 (브랜드명·푸터 표기) | 모든 페이지 | `<title>`, 로고, `.footer .biz` |
| 02 브랜드 (한줄소개·스토리·키워드·철학·사람/공간 사진) | `about.html` | 각 섹션 |
| 03 디자인 방향 (분위기·컬러·폰트) | `style.css` | 맨 위 `:root` 디자인 토큰 |
| 04 홈 (메인/서브 카피·대표 이미지·강점3가지) | `index.html` | 히어로 + `.features` |
| 05 제품 (취급품목·제품01~·구매링크) | **사이트 `/admin` → `products.json`** | 브랜드 담당자가 관리 (아래 "제품 시스템" 참고) |
| 06 포트폴리오 (사례①②③) | **사이트 `/admin` → `portfolio.json`** | 브랜드 담당자가 한 페이지로 관리 |
| 07 문의 (연락수단·운영시간) | `contact.html` | `.contact-item` (없는 항목은 삭제) |

## 제품·포트폴리오 시스템 (중요 — 학생은 UI, 브랜드는 콘텐츠)
- **브랜드 담당자**가 사이트의 `/admin`에서 `제품 관리`와 `포트폴리오` 탭을 사용합니다.
- 제품 탭에서는 제품 등록·수정·삭제, 상세페이지 내용, 외부 구매 링크를 관리합니다.
- 포트폴리오 탭에서는 한 개의 큰 편집기로 글·이미지·링크·영상을 한 페이지처럼 작성합니다.
- 한 브랜드에 두 팀 시안이 있으면 대표 관리자 한 곳의 저장 내용과 업로드 이미지가 두 저장소에 함께 반영됩니다. 두 번째 시안의 `/admin`은 대표 관리자 주소로 이동합니다.
- **학생은 제품 데이터나 관리자 기능을 만들지 않고 UI/UX만 디자인**합니다.
- 제품 데이터는 `products.json`, 포트폴리오 본문은 `portfolio.json`, 비밀번호 해시는 `admin-config.json`, 관리자 브랜드명은 `brand-config.js`, 브랜드 편집 화면은 `admin.html`·`admin.css`·`admin.js`, 저장과 인증은 `worker.js`, 안전한 공개 렌더링은 `catalog.js`가 담당합니다.
- `products.html`은 공개 제품 목록, `product.html?id=`는 브랜드가 편집기에서 작성한 제품별 상세 내용을 자동 렌더링합니다.
- 학생은 `products.html`, `product.html`, `style.css`의 제품 카드·상세페이지 CSS를 자유롭게 개선할 수 있습니다.
- 학생은 `portfolio.html`의 `#portfolioContent` 컨테이너와 `portfolio.js` 로딩을 유지하면서 `style.css`의 `.portfolio-content` UI를 자유롭게 개선할 수 있습니다.
- 제품을 HTML에 하드코딩하거나 `products.json` 필드명을 바꾸지 마세요. **데이터와 표현을 분리**하세요.
- 상세 내용은 `sections` 안의 단일 `rich_text` 본문으로 저장됩니다. 브랜드 담당자는 한 편집기에서 글·이미지·링크·영상을 구성하며, 학생은 저장 구조나 편집기를 바꾸지 않습니다.
- 공개 상세페이지에서는 편집기 본문의 제목·글·목록·링크·이미지·유튜브·비메오·직접 영상이 모바일에서도 자연스럽게 표시되어야 합니다.
- 구매 버튼은 `buyLink`가 안전한 `http://` 또는 `https://` 주소일 때만 표시되며 새 탭으로 열립니다.

## 디자인 토큰 (중요)
- 색·폰트는 **`style.css` 맨 위 `:root` 변수만 수정**해서 바꿉니다. 개별 요소에 색을 흩뿌리지 마세요.
- 예: "아이보리 배경 + 딥네이비 + 포인트 틸" → `--bg`, `--ink`, `--accent`만 조정.
- 공통 헤더/푸터를 수정하면 **5개 페이지 모두에 동일하게** 반영하세요.

## 대화
- 항상 **한국어**로 설명해 주세요.
- 큰 변경을 하기 전에는 무엇을 바꿀지 **한 줄로 먼저** 알려주세요.

## 코드 규칙
- 공개 사이트는 **순수 HTML/CSS/JS**만 사용하고 빌드도구·프레임워크를 추가하지 마세요.
- 브랜드 관리자에는 운영진이 지정한 **TOAST UI Editor 3.2.2**가 들어 있습니다. 버전·CDN 주소·무결성 값을 임의로 바꾸거나 다른 편집기를 추가하지 마세요.
- 초보자가 읽기 쉽게, 필요한 곳에 **한국어 주석**을 달아주세요.
- 이미지는 `images/` 폴더에 넣고 상대경로로 연결하세요. 페이지를 추가할 때는 기존 페이지의 헤더/푸터를 복사해 일관성을 유지하세요.

## ⭐ 모바일 최적화 (필수)
- **모바일 우선(mobile-first)**: 기본 CSS는 작은 화면용, `@media (min-width: 720px)`로 큰 화면 확장.
- 새 섹션·요소 추가 시 **좁은 화면(약 360~390px)에서 깨지지 않는지** 반드시 확인.
- 버튼·링크는 손가락으로 누르기 쉽게, 가로 스크롤이 생기지 않게.
- 상단 메뉴는 모바일에서 **햄버거(☰)**로 접히는 구조를 유지하세요.

## 건들지 말 것
- **`wrangler.jsonc`·`wrangler.admin.jsonc`·`worker.js`** — 배포·로그인·저장 API 설정입니다.
- **`products.json`** — 브랜드 담당자가 관리하는 운영 데이터입니다. 학생 작업 중 샘플 문구·제품을 하드코딩하지 마세요.
- **`portfolio.json`** — 브랜드 담당자가 관리하는 운영 포트폴리오 본문입니다. 학생이 직접 수정하지 마세요.
- **`admin-config.json`** — 관리자 비밀번호 해시와 세션 버전입니다. 직접 수정하거나 공개하지 마세요.
- **`brand-config.js`** — 관리자 화면의 브랜드명 설정입니다. 운영진 요청 없이 수정하지 마세요.
- **`catalog.js`의 보안 함수** — 외부 구매 링크·이미지·리치 텍스트를 안전하게 처리합니다.
- **`portfolio.js`의 데이터 로딩** — 관리자에서 저장한 포트폴리오를 공개 페이지에 연결합니다.
- **`admin.html`·`admin.css`·`admin.js`** — 브랜드 담당자 전용 제품·포트폴리오 편집기입니다.
- `.git` 폴더 및 배포 관련 설정.

제품 UI를 변경해야 할 때는 위 파일 대신 `products.html`, `product.html`, `style.css`를 수정하세요. 렌더링에 새 데이터가 꼭 필요하면 기존 필드를 우선 활용하고, 운영진 요청 없이 스키마를 확장하지 마세요.

## 배포
- 사이트 배포는 **GitHub의 검증을 통과해 main에 병합된 뒤** 자동으로 시작됩니다.
- 학생은 브랜치·PR·병합 명령을 직접 다룰 필요가 없습니다. 아래의 `배포해` 절차를 Codex가 수행합니다.

## 안전한 공동 작업 (최우선)
이 절은 위의 일반 규칙보다 우선합니다. 활성 학생 작업 수에 맞춰 불필요한 절차는 줄이되, 최신 코드와 운영 기능은 항상 보호합니다.

### 작업 인원 자동 판단 — 학생에게 묻지 않음
- 코드 수정, 커밋 또는 배포 전에 `git fetch --prune origin`을 실행하세요.
- 현재 브랜치를 제외하고, `origin/main`에 아직 병합되지 않은 원격 `student/*` 브랜치와 열린 `main` 대상 PR을 확인하세요.
- 다른 활성 학생 브랜치가 없으면 1인 작업, 하나 이상이면 여러 명 협업으로 자동 처리하세요. 학생에게 모드를 묻거나 `progen.team-mode`를 설정·사용하지 마세요.
- 이미 `origin/main`에 병합된 옛 브랜치는 작업 인원 판단에서 제외하세요.
- 다른 컴퓨터의 아직 push되지 않은 작업은 보이지 않으므로, 모든 새 브랜치 이름에 초 단위 시각과 무작위 4자리 영문·숫자를 넣어 최초 동시 작업도 충돌하지 않게 하세요.
- 1인 작업에서는 학생 ID 생성, 담당 페이지 확인, 다른 학생 브랜치·작업트리 추가 탐색을 생략하세요. 여러 명 협업에서만 다른 학생 작업 보존과 충돌 검사를 적용하세요.
- 두 경우 모두 최신 `origin/main` 반영, 전체 변경 비교, 보호 파일 검사, 사이트 검증, PR, `validate-site`, 병합 대기열을 생략하지 마세요.

### 작업자 식별과 브랜치
- `git config --local --get progen.student-name`을 확인하세요.
- 이름이 없으면 “이 컴퓨터에서 사용할 이름 또는 번호를 알려주세요. 한 번만 저장할게요.”라고 한 번만 물으세요.
- 답을 받으면 이름은 `progen.student-name`과 로컬 `user.name`에 저장하세요. 인증 계정·토큰·`user.email`은 바꾸지 마세요.
- 여러 명 협업에서만 `progen.student-id`를 확인하고, 없으면 충돌하지 않는 짧은 영문 ID(예: `s-a1b2c3d4`)를 생성해 로컬 Git 설정에 저장하세요.
- 브랜치는 1인 작업이면 `student/solo/<YYYYMMDD-HHmmss>-<무작위4자리>-<작업요약>`, 여러 명 협업이면 `student/<학생ID>/<YYYYMMDD-HHmmss>-<무작위4자리>-<작업요약>`를 사용하세요. 커밋과 PR 제목은 `[학생이름] 작업 요약` 형식으로 만드세요.

### 작업 시작 시 자동 최신화
- 코드 수정 전 항상 `git fetch --prune origin`을 실행하세요.
- 깨끗한 `main`이면 `git pull --ff-only origin main`으로 최신화한 뒤 작업하세요.
- 작업 브랜치가 깨끗하면 최신 `origin/main`을 먼저 반영하세요.
- 수정 중인 파일이 있으면 먼저 안전한 학생 브랜치에 보존하고, `reset --hard`, 강제 checkout, force push로 작업을 지우지 마세요.
- 다른 활성 학생 브랜치가 있을 때는 다른 학생의 열린 작업트리·브랜치·파일을 삭제하거나 임의로 바꾸지 마세요.

### “배포해”라고 하면 Codex가 할 일
학생에게 Git 명령을 시키지 말고 아래를 끝까지 직접 수행하세요.

1. 활성 학생 브랜치 수와 이름을 확인하고, 현재 수정분 전체를 자동 판단한 작업 브랜치에 안전하게 보존합니다.
2. `git fetch --prune origin` 후 공통 조상부터 현재까지의 **전체 변경분**을 확인합니다. 마지막 커밋만 비교하지 마세요.
3. 아래 보호 파일이 바뀌지 않았는지 검사하고 최신 `origin/main`을 현재 작업에 반영합니다.
4. 충돌이 나면 양쪽 의도와 최신 운영 데이터를 확인해 함께 살리는 방향으로 해결합니다. 같은 문구·디자인을 동시에 바꿔 판단할 근거가 없을 때만 배포를 멈추고 학생에게 선택을 묻습니다.
5. 변경된 페이지뿐 아니라 공통 헤더·푸터, 링크, 이미지, 모바일 화면, JavaScript/JSON 문법과 기존 기능 회귀를 확인합니다.
6. 검증이 통과하면 학생 브랜치만 push하고 `main` 대상 PR을 생성하거나 기존 PR을 재사용합니다. PR 본문에는 학생 이름, 변경 목적, 변경 파일, 실행한 검사를 기록합니다.
7. `gh pr merge --auto --squash`로 자동 병합을 켭니다. GitHub 병합 대기열과 `validate-site` 검사가 최신 main과의 조합을 다시 확인한 뒤 순서대로 병합하게 하세요.
8. PR이 병합될 때까지 상태를 확인합니다. 실패하면 main에 우회 push하지 말고 원인을 수정해 같은 PR에 다시 올립니다.
9. 병합 뒤 `git fetch --prune origin`으로 원격 상태를 갱신합니다. 현재 작업트리를 강제로 초기화하지 말고 새 작업은 최신 `origin/main`에서 시작하게 하세요.
10. 마지막 답변에는 학생 이름, PR 주소, 검사 결과, 병합 여부만 쉽게 알려주세요.

`gh`가 없거나 인증되지 않았다면 main에 직접 push하지 마세요. 학생 브랜치와 커밋을 그대로 보존한 뒤 GitHub CLI 로그인만 최초 1회 안내하고, 인증 후 나머지 절차를 계속 수행하세요.

### 학생이 수정하면 안 되는 보호 파일
- `.github/**`, `AGENTS.md`, `STUDENT_GUIDE.md`, `RULES.md`
- `.assetsignore`, `wrangler.jsonc`, `wrangler.admin.jsonc`, `worker.js`
- `products.json`, `portfolio.json`, `admin-config.json`, `brand-config.js`
- `admin.html`, `admin.css`, `admin.js`, `catalog.js`, `portfolio.js`

보호 파일이 학생 변경분에 포함되면 배포하지 말고 원래 파일을 최신 `origin/main` 상태로 되돌리되, 다른 학생 파일이나 운영 데이터까지 함께 되돌리지 마세요.
